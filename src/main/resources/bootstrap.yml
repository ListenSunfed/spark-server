spring:
  application:
    name: spark-server
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
  datasource:
    url: jdbc:postgresql://datalake-master:5432/datalaker
    username: postgres
    password: postgres
################### Logback Configuration
log:
  home: logs